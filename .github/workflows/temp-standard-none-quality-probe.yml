name: Temporary STANDARD none quality probe

on:
  push:
    branches:
      - perf/standard-latency-phase2-20260828
    paths:
      - scripts/temp_standard_none_quality_probe.mjs
      - .github/workflows/temp-standard-none-quality-probe.yml

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production-gpuhub
    env:
      GPUHUB_SSH_HOST: ${{ secrets.GPUHUB_SSH_HOST }}
      GPUHUB_SSH_PORT: ${{ secrets.GPUHUB_SSH_PORT }}
      GPUHUB_SSH_USER: ${{ secrets.GPUHUB_SSH_USER }}
      GPUHUB_SSH_PRIVATE_KEY: ${{ secrets.GPUHUB_SSH_PRIVATE_KEY }}
      GPUHUB_SSH_KNOWN_HOSTS: ${{ secrets.GPUHUB_SSH_KNOWN_HOSTS }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Configure pinned SSH identity
        shell: bash
        run: |
          set -euo pipefail
          for name in GPUHUB_SSH_HOST GPUHUB_SSH_PORT GPUHUB_SSH_USER GPUHUB_SSH_PRIVATE_KEY GPUHUB_SSH_KNOWN_HOSTS; do test -n "${!name:-}"; done
          umask 077
          mkdir -p "$HOME/.ssh"
          printf '%s\n' "$GPUHUB_SSH_PRIVATE_KEY" > "$HOME/.ssh/id_ed25519"
          printf '%s\n' "$GPUHUB_SSH_KNOWN_HOSTS" > "$HOME/.ssh/known_hosts"
          chmod 600 "$HOME/.ssh/id_ed25519" "$HOME/.ssh/known_hosts"
      - name: Compare STANDARD default and no-thinking quality
        shell: bash
        run: |
          set -euo pipefail
          remote="${GPUHUB_SSH_USER}@${GPUHUB_SSH_HOST}"
          opts=(-i "$HOME/.ssh/id_ed25519" -p "$GPUHUB_SSH_PORT" -o BatchMode=yes -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes)
          ssh "${opts[@]}" "$remote" 'cat > /tmp/div3rsa-standard-none-quality-probe.mjs' < scripts/temp_standard_none_quality_probe.mjs
          ssh "${opts[@]}" "$remote" 'set -e; trap "rm -f /tmp/div3rsa-standard-none-quality-probe.mjs" EXIT; curl --fail --silent --show-error --max-time 5 http://127.0.0.1:6006/health >/dev/null; /root/autodl-tmp/localai/runtime/node-current/bin/node /tmp/div3rsa-standard-none-quality-probe.mjs'
