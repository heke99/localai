# Production Agent Runtime

This branch wires the agent runtime end-to-end: skill loading, knowledge retrieval, model tool calling, guarded tool execution, sandboxing, verification, and learning hooks. Implementation files and tests are committed separately on this branch.
